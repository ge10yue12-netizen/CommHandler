---
description: 串口/网口互斥；全协议跟库能力；指令与测数分离
alwaysApply: true
---

# 项目核心

| 端 | 角色 | DLL |
|----|------|-----|
| CommLab | 软件 | 是，同一时间只开串口或网口 |
| MachinePeer | 试验机工具 | 否 |

## 原则

- **跟库设计**：试验机发指令/测数 → 库解析并抛信号/自动 ACK → 软件只做库已有对应动作。  
- **库没有的路径不管**：不发明入站解析、不假装业务回发。  
- **指令 ≠ 测数**：开始/停止等不连带发力温；回发仅在收到测数且库有 `SendData(vector)` 分支时。
- **试验机解析回包**：软件业务回发后，Peer 按库出站格式解出力/温（`ReplyParse.h`）；库 ACK ≠ 业务结果。

## 网口能力（库 `SocketComm`）

| PT | 收指令 | 收测数 | 业务回发 |
|----|--------|--------|----------|
| 0 JSON | 是 | **否** | 是（出站 tn=2） |
| 1 万测 | 是 | **否** | 是 |
| 2 中机 | 是 | 是 | 是 |
| 3 三思 | **否** | 是 | **否** |
| 4 触发存图 | 存图 | **否** | **否** |
| 5 威盛 | **否** | 是 | **否** |
| 6/7 纳百川 | 是 | **否** | 是 |

## 串口能力（库 `SerialPortComm`）

| PT | 收指令 | 收测数 | 业务回发 |
|----|--------|--------|----------|
| 0 三思 | 是 | 是 | 是 |
| 1 科新 | **否** | 是 | 是 |
| 2 时代新材 | **否** | 是 | **否** |
| 3 IEEE | 是 | **否** | 是 |
| 4 冠腾 | **否** | **否** | 是 |

门控实现：`ProtoCapability.h`；组包：`DemoComm.h`。

不改动态库。改文件须「确认修改」；提交须「确认提交」。
